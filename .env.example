# Copy to `.env` and fill values.
#
# Supabase Postgres:
#
# IMPORTANT: Many Supabase projects are IPv6-only for "Direct connection".
# If you see "Not IPv4 compatible" in Supabase, use "Session Pooler" and copy its host/port/user.
#
# Direct connection (may fail on IPv4-only networks):
# DB_HOST="db.<YOUR_PROJECT_REF>.supabase.co"
# DB_PORT="5432"
# DB_USER="postgres"
#
# Session Pooler (recommended if Direct connection fails):
DB_HOST="<POOLER_HOST_FROM_SUPABASE>"
DB_PORT="<POOLER_PORT_FROM_SUPABASE>"   # often 6543
DB_USER="<POOLER_USER_FROM_SUPABASE>"   # often includes project ref
DB_NAME="postgres"
DB_PASSWORD="<YOUR_DB_PASSWORD>"
DB_SSLMODE="require"
# Disable SQLAlchemy client pooling for PgBouncer/pooler:
DB_DISABLE_SQLALCHEMY_POOL="1"
#
# Alternatively, you can provide a full SQLAlchemy URL:
# DATABASE_URL="postgresql+psycopg2://postgres:<URL_ENCODED_PASSWORD>@db.<YOUR_PROJECT_REF>.supabase.co:5432/postgres?sslmode=require"
#
# Optional (not used by this API yet, but commonly needed later)
SUPABASE_URL="https://<YOUR_PROJECT_REF>.supabase.co"
SUPABASE_ANON_KEY="<YOUR_SUPABASE_ANON_KEY>"

