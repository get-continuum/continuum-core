# API reference (OpenAPI)

Run the server and open the built-in docs:

- Swagger UI: `/docs`
- ReDoc: `/redoc`

For the static docs site, we recommend linking to a hosted OpenAPI UI that points at your deployed API base URL.

