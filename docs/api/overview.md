# API overview

Continuum exposes two primary contracts:

- **Intent resolution**: `POST /metrics/resolve_intent`
- **Typed contract resolution**: `POST /metrics/{metric_id}/resolve`

Health:

- `GET /health`

